'use strict';
// The greetUser function, which takes one parameter - 
// and returns a string like this: "Hello, [NAME]!". I also added quotes.
function greetUser(name) {
    return `"Привіт, ${name}!"`;
}
console.log(greetUser('Влад'));
