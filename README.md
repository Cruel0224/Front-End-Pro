DZ 58. Write a todo-list program.

The user sees a list of all to-dos (done to undone). When clicking on a specific to-do, its state switches to the opposite.

Below the list is a form with an input and a button. The user can enter the task they want to do in the input and press the button. After clicking, a new task appears in the list.
